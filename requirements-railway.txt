# ============================================================================
# KUMON AI ASSISTANT - RAILWAY DEPLOYMENT OPTIMIZED DEPENDENCIES
# ============================================================================
# Railway-specific requirements with PyTorch CPU-only optimization
# Generated: 2025-01-14
# Strategy: CPU-only PyTorch, memory optimized, fast Railway builds
# ============================================================================

# INCLUDE BASE PRODUCTION REQUIREMENTS
-r requirements-production.txt

# RAILWAY-SPECIFIC OVERRIDES
# PyTorch CPU-only versions (overrides base requirements)
--index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu  
torchaudio==2.5.1+cpu

# Railway build optimization packages
wheel==0.42.0
setuptools==69.0.3
pip==24.0